{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // outDir/rootDir อยู่เฉพาะ config ที่ emit จริง (tsconfig.json ใช้แค่ typecheck --noEmit)
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
