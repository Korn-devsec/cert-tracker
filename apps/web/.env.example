# ===== apps/web =====
# คัดลอกเป็น .env (ไฟล์ .env ถูก gitignore ไว้ — ห้าม commit)
# หมายเหตุ: ตัวแปรที่ขึ้นต้นด้วย VITE_ จะถูก inline ลง bundle → ห้ามใส่ secret

VITE_API_BASE_URL=http://localhost:3000
