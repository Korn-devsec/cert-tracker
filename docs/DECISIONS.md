# DECISIONS — บันทึกการตัดสินใจสำคัญของโปรเจกต์

> ทุกครั้งที่มีการตัดสินใจสำคัญ (เปลี่ยน schema, เพิ่ม library, เปลี่ยนแนวทาง)
> ให้บันทึกลงตารางนี้ พร้อมเหตุผล

| วันที่ | หัวข้อ | ตัดสินใจ | เหตุผล |
|---|---|---|---|
| 2026-08-03 | Monorepo tool | ใช้ **npm workspaces** (`packages/*`, `apps/*`) ไม่เพิ่ม pnpm/Turborepo | Node 20 + npm 11 มีอยู่แล้ว ไม่ต้องเพิ่ม tool ใหม่ และโครงสร้างใน CLAUDE.md เป็น monorepo 3 package ซึ่ง workspaces เอาอยู่ |
| 2026-08-03 | รูปแบบ build ของ `packages/shared` | คอมไพล์ 2 แบบ: `dist/cjs` (สำหรับ api = CommonJS) และ `dist/esm` (สำหรับ web = Vite/ESM) ผ่าน `exports` map | NestJS รันเป็น CommonJS แต่ Vite เป็น ESM ถ้า build แบบเดียวจะมีฝั่งใดฝั่งหนึ่ง resolve ไม่ได้ มี test ทั้งสองฝั่ง (`shared-package.spec.ts`) กันพังตอน refactor |
| 2026-08-03 | Test runner | api ใช้ **Jest** (convention ของ NestJS), `packages/shared` + web ใช้ **Vitest** | Jest คือค่า default ของ NestJS schematics ส่วน Vitest ทำงานร่วมกับ Vite/ESM ได้โดยไม่ต้องตั้ง transform เพิ่ม |
| 2026-08-03 | เวอร์ชัน Vitest | ล็อก `vitest@^3` (ไม่ใช้ ^2) | Vitest 2 peer-depend กับ Vite 5 ทำให้ npm ติดตั้ง Vite ซ้อนอีกชุด แล้ว type ของ plugin ชนกันจน `tsc -b` ฝั่ง web ไม่ผ่าน |
| 2026-08-03 | Host port ของ PostgreSQL | map `5433:5432` (ปรับได้ด้วย `POSTGRES_PORT` ใน `.env` ที่ root) | เครื่อง dev มี PostgreSQL local ครองพอร์ต 5432 อยู่แล้ว ถ้า map 5432 ตรงๆ container จะ start ไม่ขึ้น |
| 2026-08-03 | พฤติกรรมเมื่อต่อ DB ไม่ได้ | `PrismaService.onModuleInit()` log error แต่ไม่ throw, `GET /health` ตอบ HTTP 503 + `{ status: "error", db: "disconnected" }` | ถ้า throw แอปจะตายตอน boot แล้ว health check ก็ตอบอะไรไม่ได้ วินิจฉัยยาก แยก 200/503 ให้ monitoring จับได้ตรงๆ |
| 2026-08-03 | ขอบเขต enum ใน `packages/shared` | นอกจาก 4 ตัวใน checklist (`RiskLevel`, `WorkStatus`, `NotificationTier`, `HistoryAction`) เพิ่ม `NotificationChannel`, `UserRole`, `ImportStatus` และ label ภาษาไทยของแต่ละ enum | 3 ตัวที่เพิ่มมาจาก Data Model/RBAC ใน CLAUDE.md อยู่แล้ว และ Phase 1 (Prisma schema) ต้องใช้ทันที ส่วน label ภาษาไทยเก็บที่เดียวกันเพื่อให้ Dashboard และ Export ใช้ค่าเดียวกัน |
| 2026-08-03 | `docker-compose.yml` ใน Phase 0 | มีแค่ service `db` | Phase 0 ต้องการแค่ DB ให้ dev รันได้ ส่วน image ของ api/web (nginx) เป็นงานของ Phase 8 ตาม PLAN.md |
| 2026-08-03 | tsconfig `moduleResolution` | ใช้ `nodenext` (api และ shared/cjs) แทน `node`/`node10` และย้าย `outDir`/`rootDir` ไปไว้ใน config ที่ emit จริง | `node10` และ `baseUrl` ถูก deprecate ใน TypeScript 7 (IDE ขึ้น error) — `nodenext` ยัง emit CommonJS เหมือนเดิมเพราะ package ไม่ได้ตั้ง `"type": "module"` |
| 2026-08-03 | **[Phase 1]** cert ที่หมดอายุแล้ว (`daysUntilExpiry` ติดลบ) | `RiskLevel` = `HIGH` — **ไม่เพิ่มค่า `EXPIRED`** และแยกด้วยฟังก์ชัน `isExpired(days)` ต่างหาก | การ์ดสรุปในดีไซน์เดิมมี 4 ระดับพอดี ถ้าเพิ่มค่าที่ 5 ต้องรื้อทั้งการ์ด/Doughnut/badge — ส่วน Dashboard ต้องการตัวนับ "Expired" แยกอยู่แล้วจึงใช้ `isExpired()` คำนวณตอน query ได้ (ปิดประเด็นที่ค้างจาก Phase 0) |
| 2026-08-03 | **[Phase 1]** `daysUntilExpiry` / `riskLevel` ใน DB | **ไม่เก็บเป็นคอลัมน์** — คำนวณจาก `expiresAt` ตอน query ด้วย `calculateRisk()` ใน `packages/shared` | ตรงตาม CLAUDE.md (`*` = คำนวณได้) ถ้าเก็บไว้ค่าจะค้างและเพี้ยนทันทีที่ข้ามวัน ทำให้ Dashboard โกหก |
| 2026-08-03 | **[Phase 1]** วิธีนับจำนวนวันคงเหลือ | เทียบเป็น "วันปฏิทิน" บนฐาน UTC (ตัดเวลาในวันออก) ด้วย `calculateDaysUntilExpiry()` | ผลลัพธ์นิ่งตลอดวัน (ไม่แกว่งตามเวลาที่เรียก) และตรงกับความหมายของคอลัมน์ `Days Until` ในไฟล์ Excel จริง |
| 2026-08-03 | **[Phase 1]** `Certificate.endpoint` | เป็น `String @default("")` (ไม่ใช่ nullable) + unique key `(companyId, commonName, endpoint)` | PostgreSQL ถือว่า `NULL` แต่ละตัวไม่ซ้ำกัน ถ้าปล่อยเป็น nullable unique key ที่ Phase 3 ใช้ upsert จะไม่ทำงานเลย → import ซ้ำจะสร้างข้อมูลซ้ำ |
| 2026-08-03 | **[Phase 1]** ชื่อ table | ไม่ใช้ `@@map` — ชื่อ table ตรงกับชื่อ model (`Certificate`, `HistoryLog`) | ค่า default ของ Prisma ลดจุดที่ต้องดูแลให้ตรงกัน 2 ที่ ระบบไม่มีการเขียน raw SQL (นอกจาก `SELECT 1` ใน health check) |
| 2026-08-03 | **[Phase 1]** พฤติกรรมตอนลบข้อมูล | `Cascade` เมื่อลบ Company/Certificate (ลูกทั้งหมดหายตาม), `SetNull` สำหรับ `Certificate.siteId` และ `RenewalTask.assigneeId` | ลบบริษัทต้องไม่เหลือ cert ลอย ส่วนการลบ site หรือปิดบัญชีพนักงานต้องไม่ทำให้ cert/งานหายไปด้วย — ในทางปฏิบัติ Phase 2 ใช้ soft delete (`isActive`) เป็นหลักอยู่แล้ว |
| 2026-08-03 | **[Phase 1]** Hash รหัสผ่าน | ใช้ **scrypt จาก `node:crypto`** (`apps/api/src/common/password.ts`) เก็บเป็น `scrypt$<salt>$<key>` — ไม่เพิ่ม bcrypt/argon2 | ยังไม่ได้ตกลงเรื่อง library เพิ่ม จึงเลือกของที่มีใน Node (scrypt เป็น KDF memory-hard ที่ OWASP ยอมรับ) รูปแบบมี prefix scheme อยู่แล้วจึงเปลี่ยนภายหลังได้โดยไม่ต้อง migrate ข้อมูลทันที — **ขอให้ยืนยันตอน Phase 2 (Auth)** |
| 2026-08-03 | **[Phase 1]** idempotency ของ NotificationLog | เพิ่มคอลัมน์ `sentOn` (`@db.Date`) + unique `(certificateId, tier, channel, sentOn)` | รองรับกติกา 2 แบบในตารางเดียว: tier 90/60/30 เช็คว่าเคยส่ง tier นั้นแล้วหรือยัง ส่วน tier ≤7 วัน ส่งได้วันละ 1 ครั้ง โดยให้ DB เป็นคนกันซ้ำ (กันกรณี cron รันซ้อน) |
| 2026-08-03 | **[Phase 1]** ที่ตั้งค่า seed ของ Prisma | คงไว้ที่ `package.json#prisma.seed` ยังไม่ย้ายไป `prisma.config.ts` | Prisma 6 ขึ้น warning ว่าจะย้ายใน Prisma 7 แต่การย้ายตอนนี้ทำให้ Prisma เลิกโหลด `.env` ให้อัตโนมัติ (ต้องจัดการ dotenv เอง) → เก็บไว้ทำพร้อมกับตอนอัป Prisma 7 |

| 2026-08-03 | **[Phase 2]** วิธี implement JWT | ใช้ `@nestjs/jwt` + guard ที่เขียนเอง — **ไม่ใช้ passport** (`@nestjs/passport`, `passport`, `passport-jwt`, `@types/passport-jwt`) | Tech stack ล็อกไว้แค่ "JWT + RBAC" ไม่ได้ระบุ passport — การใช้ JwtService ตรงๆ ลด dependency 4 ตัว และ guard ที่เขียนเองสั้นกว่า strategy ของ passport อีก (ระบบนี้มี auth แบบเดียว ไม่มี OAuth/social login) |
| 2026-08-03 | **[Phase 2]** ขอบเขตของ guard | `JwtAuthGuard` + `RolesGuard` ลงทะเบียนเป็น **global guard** (`APP_GUARD`) → ทุก endpoint ปิดเป็นค่าเริ่มต้น ต้องติด `@Public()` เพื่อเปิด (มีแค่ `POST /auth/login` กับ `GET /health`) | ถ้าใช้ guard แบบใส่ทีละ controller วันหนึ่งจะมีคนลืมใส่แล้วข้อมูลลูกค้าหลุดโดยไม่มีใครรู้ — ปิดเป็น default แล้วเปิดเฉพาะที่จำเป็นทำให้ "ลืม" กลายเป็น fail-safe |
| 2026-08-03 | **[Phase 2]** การแบ่งสิทธิ์ RBAC | endpoint ที่ไม่ประกาศ `@Roles()` = อ่านได้ทุก role ที่ login แล้ว · Company create/update/delete = `ADMIN` · Site create/update/delete = `ADMIN` + `OPERATOR` · `POST /auth/register` = `ADMIN` | ตรงกับเกณฑ์ "viewer ทำได้แค่อ่าน" — ส่วน site เป็นข้อมูลระดับปฏิบัติงานที่ operator ต้องจัดการเองได้ ไม่ควรต้องรอ admin |
| 2026-08-03 | **[Phase 2]** ตรวจ user ซ้ำทุก request | `JwtAuthGuard` query user จาก DB ทุกครั้ง (ไม่เชื่อ payload ใน token อย่างเดียว) | บัญชีที่ถูกปิดใช้งานต้องใช้ token ที่ออกไปแล้วต่อไม่ได้ทันที (มี e2e test คุมไว้) — แลกกับ query เบาๆ 1 ครั้งต่อ request ซึ่งคุ้มกว่าการทำ token blacklist |
| 2026-08-03 | **[Phase 2]** ใช้ enum จากที่ไหนในฝั่ง api | ฝั่ง api ใช้ enum จาก `@prisma/client` ส่วนฝั่ง web ใช้จาก `@cert-tracker/shared` | Prisma generate enum เป็น string union แต่ shared เป็น TS enum — assign ข้ามกันตรงๆ TypeScript ไม่ยอม ถ้าฝืนใช้ต้อง cast ทุกจุด · มี `enum-parity.spec.ts` คุมว่าค่าสองฝั่งตรงกันเสมอ |
| 2026-08-03 | **[Phase 2]** `Company.code` | แก้ไม่ได้หลังสร้าง (ไม่มีใน `UpdateCompanyDto`) และบันทึกเป็นตัวพิมพ์ใหญ่เสมอ | code เป็นรหัสที่ผู้ใช้เลือกตอน import ถ้าเปลี่ยนภายหลัง ประวัติการ import ย้อนหลังจะอ่านไม่รู้เรื่องว่าเป็นบริษัทเดียวกัน |
| 2026-08-03 | **[Phase 2]** การลบ Site | ลบจริง (ไม่ใช่ soft delete) แต่ถ้ายังมี certificate ผูกอยู่ → **409** ให้ย้าย cert ก่อน | Site เป็นแค่ชั้นจัดกลุ่ม optional ไม่มีประวัติของตัวเอง จึงไม่ต้องมี `isActive` — แต่ปล่อยให้ลบแล้ว `siteId` ของ cert กลายเป็น null เงียบๆ คือการทำข้อมูลหายโดยผู้ใช้ไม่รู้ตัว |
| 2026-08-03 | **[Phase 2]** เพิ่มค่า HistoryAction | เพิ่ม `SITE_DELETED`, `USER_CREATED`, `USER_UPDATED` (migration `20260803095940_...`) | ตอน Phase 0 ตกหล่นไป ทำให้ Phase 2 ไม่มี action ที่ตรงความหมายสำหรับลบ site และสร้างผู้ใช้ — เลี่ยงการเอา `SITE_UPDATED` มาใช้ผิดความหมาย (`USER_UPDATED` เตรียมไว้ให้หน้า Settings/Users ใน Phase 7) |
| 2026-08-03 | **[Phase 2]** ข้อความตอน login ไม่ผ่าน | ตอบข้อความเดียวกัน (`อีเมลหรือรหัสผ่านไม่ถูกต้อง`) ทั้งกรณีไม่มีอีเมล / รหัสผิด / บัญชีถูกปิด | กัน user enumeration — ถ้าแยกข้อความ คนนอกจะไล่เดาได้ว่าอีเมลไหนมีบัญชีในระบบ (มี unit test เทียบว่าข้อความตรงกันจริง) |
| 2026-08-03 | **[Phase 2]** ฐานข้อมูลที่ใช้ทดสอบ e2e | ใช้ DB dev ตัวเดิม แต่ข้อมูลทดสอบใช้ prefix `e2e-` / `E2E` และลบทิ้งใน `afterAll` | ยังไม่ตั้ง DB แยกเพราะต้องดูแล migration 2 ที่ · `npm test` (unit) ไม่แตะ DB เลย ส่วน `npm run test:e2e` ต้องมี DB รันอยู่ — ถ้าภายหลัง e2e ใหญ่ขึ้นให้แยก database ต่างหาก |

| 2026-08-03 | **[Phase 3]** `endpoint` หลายค่าในเซลล์เดียว | **แตกเป็นหลาย Certificate record** (record ละ 1 endpoint) ไม่เก็บเป็น array | ตรงกับ Data Model ใน CLAUDE.md ที่ `endpoint` เป็น scalar (ต่างจาก `san[]`) และตรงกับคีย์ upsert ที่ PLAN.md Phase 3 กำหนดไว้ (`companyId+commonName+endpoint`) · แต่ละ endpoint ต่ออายุ/ติดตั้งแยกกันได้จริง จึงควรมีงานและประวัติของตัวเอง (ปิดประเด็นที่ค้างจาก Phase 1) |
| 2026-08-03 | **[Phase 3]** `Expires` ที่ไม่มี timezone | ตีความเป็น **UTC** (`2026-09-18T12:25:54` → `...Z`) ไม่ใช่เวลาเครื่อง | `notAfter` ใน X.509 เป็น UTC เสมอ และถ้าใช้ `new Date(text)` ผลจะเปลี่ยนตาม timezone ของเครื่องที่รัน ทำให้ import เครื่องละค่า |
| 2026-08-03 | **[Phase 3]** รูปแบบวันที่ที่ไม่รองรับ | `18/09/2026` → **error รายแถว** ไม่เดา | กำกวมกับ `09/18/2026` (วัน/เดือนสลับ) เดาผิดแล้วได้วันหมดอายุผิดหลายเดือน ซึ่งอันตรายกว่าการให้คนไปแก้ไฟล์ · ที่รองรับ: ISO (มี/ไม่มีเวลา, มี/ไม่มี offset), `18-Sep-2026`, Excel serial, Date cell |
| 2026-08-03 | **[Phase 3]** `อยู่ระหว่างดำเนินการ` map เป็นอะไร | `WorkStatus.NEW` (ไม่ใช่ `IN_PROGRESS` ที่ตรงตัวอักษร) | PLAN.md ระบุ "task status เริ่มต้นตาม workflow" — ในรายงานเดิมข้อความนี้หมายถึง "ยังไม่เสร็จ" ไม่ได้ยืนยันว่ามีคนรับงานแล้ว ถ้า map เป็น IN_PROGRESS เท่ากับกุว่ามีคนลงมือทำ ทำให้ board หลอกตา |
| 2026-08-03 | **[Phase 3]** `Days Until Expiry` จากไฟล์ | **ไม่เก็บลง DB** ใช้เพียง (ก) fallback คำนวณ `expiresAt` เมื่อไฟล์ไม่มีคอลัมน์ `Expires` (ก) เก็บใน metadata ของ HistoryLog เพื่อตรวจย้อนหลัง | ตรวจไฟล์จริงพบว่าค่านี้คำนวณจาก **วันสแกน** ไม่ใช่วันในชื่อรายงาน (sheet Jun เขียน "Report on 17-Jun-2026" แต่เลข 80/30 คำนวณจาก 2026-06-30) → เป็นค่าที่เก่าตั้งแต่อยู่ในไฟล์ ยืนยันการตัดสินใจใน Phase 1 |
| 2026-08-03 | **[Phase 3]** import กับ task ที่มีอยู่แล้ว | import **ไม่แก้สถานะ task เดิม** — สร้าง task ใหม่เฉพาะเมื่อไม่มีงานค้าง · ไฟล์บอก `Completed` → สร้าง task สถานะ COMPLETED (ถ้ายังไม่มี) · ไฟล์บอกยังไม่เสร็จ → สร้าง `NEW` เฉพาะเมื่อ risk ≠ SAFE | เมื่อระบบเป็นเจ้าของงานแล้ว การเปลี่ยนสถานะเป็นเรื่องของคนผ่าน workflow (Phase 4) ไฟล์รายงานรายเดือนไม่ควรกระโดดข้ามขั้นให้เงียบๆ · การสร้าง task COMPLETED ทำให้เกิดเคส "cert เหลือ 20 วัน = High แต่ Status = Completed" ที่ spec ต้องการ |
| 2026-08-03 | **[Phase 3]** sheet `Report` ในไฟล์จริง | ถือว่า **import ได้** (ไม่ได้ block) แต่ auto-suggest จะไม่เลือก — เรียงลำดับคำแนะนำด้วย จำนวนฟิลด์ที่ map ได้ → จำนวนแถว → ลำดับในไฟล์ | ตอนแรกเข้าใจว่าเป็นหน้าปกเปล่า แต่ตรวจไฟล์จริงพบว่าเป็นตารางข้อมูลที่ merge cell ไว้ (header ซ้ำหลายคอลัมน์และซ้ำ 2 แถว) เพียงแต่ขาด Owner/Status · การ block ตามชื่อ sheet จะพังกับไฟล์อื่น จึงใช้ "ความครบของคอลัมน์" เป็นเกณฑ์ และคืน sheet ทั้งหมดให้ผู้ใช้เลือกเองได้เสมอ |
| 2026-08-03 | **[Phase 3]** header ที่ merge แนวตั้ง | ข้ามแถวที่ค่าทั้งแถวซ้ำกับแถว header (`isRepeatedHeaderRow`) | sheet `Report` อ่าน header ได้ทั้งแถว 7 และ 8 เพราะ merge — ถ้าไม่ข้าม จะได้ certificate ปลอมชื่อ "Common Name" เข้าฐานข้อมูล (ครอบกรณีรายงานที่พิมพ์ header ซ้ำกลางตารางด้วย) |
| 2026-08-03 | **[Phase 3]** การ update ตอน upsert | อัปเดตเฉพาะฟิลด์ที่ไฟล์มีค่า — ค่าที่ไฟล์ว่างจะ**ไม่**ไปล้างข้อมูลเดิม (`definedOnly`) | ไฟล์รายเดือนบางเดือนไม่มี Owner (พบจริงในไฟล์) ถ้าปล่อยให้เขียน null ทับ ข้อมูลที่คนกรอกเพิ่มในระบบจะหายทุกครั้งที่ import |
| 2026-08-03 | **[Phase 3]** โหมด strict | ค่าเริ่มต้น `strict=true` → มีแถวเสียแม้แถวเดียว reject ทั้งไฟล์ · ส่ง `strict=false` เพื่อบันทึกเฉพาะแถวที่ใช้ได้ (สถานะ `PARTIAL`) | ตาม PLAN.md — ป้องกันการ import บางส่วนแบบเงียบๆ ซึ่งเป็นปัญหาที่ระบบนี้เกิดมาเพื่อแก้ |
| 2026-08-03 | **[Phase 3]** เพิ่ม `dryRun` | `POST /imports` รับ `dryRun=true` → ตรวจ + คืน preview พร้อม `action: create/update` โดยไม่เขียน DB | หน้า Import ใน Phase 7 ต้อง "แสดง preview ผล mapping → confirm" ทำที่ layer API ตอนนี้ถูกกว่าย้อนมาแก้ทีหลัง |
| 2026-08-03 | **[Phase 3]** `@types/node` กับ ExcelJS | cast Buffer จุดเดียวใน `excel/load-workbook.ts` (ไม่ลด @types/node ลงเป็น 20) | ExcelJS ประกาศ `Buffer` แบบไม่มี generic ชนกับ `Buffer<ArrayBufferLike>` ของ @types/node 22 · ลอง downgrade เป็น 20 แล้วแย่กว่าเพราะเกิด @types/node สองเวอร์ชันในโปรเจกต์เดียว |
| 2026-08-03 | **[Phase 3]** อักขระช่องว่างที่มองไม่เห็น | รวมการจัดการไว้ที่ `excel/text.ts` (NBSP/figure space/narrow NBSP → ช่องว่างปกติ, ZWSP/ZWNJ/ZWJ/BOM → ลบ) และเขียนเป็น escape `\uXXXX` ไม่วางอักขระจริงในซอร์ส | ไฟล์ Excel ที่คน copy จากเว็บมักมี NBSP ทำให้ชื่อ header เทียบไม่ตรงทั้งที่ตาเห็นว่าเหมือนกัน · โค้ดเดิมเขียน `.replace(/ /g, ' ')` ด้วยช่องว่างปกติทั้งสองตัว = no-op ที่ดูเหมือนทำงาน |
| 2026-08-03 | **[Phase 3]** build ของ apps/api | ปิด `incremental` **และ** ตั้ง `deleteOutDir: false` + เพิ่มสคริปต์ `npm run clean` | `deleteOutDir: true` ทำให้พังสองแบบบน Windows: (1) `.tsbuildinfo` รอดจากการลบ → tsc ข้าม emit ไฟล์ที่ไม่ได้แก้ → `dist` ขาดไฟล์เงียบๆ (2) `ENOTEMPTY: rmdir dist\imports\dto` ตอน start:dev · ยอมให้มีไฟล์เก่าค้างใน dist แลกกับ build ที่ทำงานได้จริงทุกครั้ง |
| 2026-08-03 | **[Phase 3]** การรัน e2e | `test:e2e` เพิ่ม `--runInBand` และทุก spec ต้อง cleanup แบบเจาะจง id ที่ตัวเองสร้าง | jest รันหลาย spec ขนานกันบน DB เดียว — cleanup เดิมของ Phase 2 ที่ลบด้วย `code startsWith 'E2E'` ไปลบข้อมูลของ spec อื่นกลางการทดสอบ ทำให้ผลเทสต์หลอก (เห็นเป็นบั๊กใน service ทั้งที่เป็นบั๊กในเทสต์) |

| 2026-08-03 | **[Phase 4]** วิธีกรองตาม `riskLevel` / เดือน / หมดอายุ | **แปลงเป็นช่วงวันของ `expiresAt` แล้วกรองใน DB** ด้วย `riskExpiryWindow()` ที่เพิ่มใน `packages/shared` (คู่ตรงข้ามของ `calculateRisk`) ไม่ดึงทุกแถวมากรองใน JS | `daysUntilExpiry` เป็นฟังก์ชันที่ลดลงตาม `expiresAt` เสมอ เงื่อนไขจำนวนวันจึงเทียบเท่าเงื่อนไขวันที่ — ถ้ากรองใน JS หลังแบ่งหน้า `total`/จำนวนหน้าจะผิด และถ้าดึงทั้งตารางมากรองก็พังตอนข้อมูลโต · มีเทสต์ในชุด shared ยืนยันว่าขอบของทั้ง 4 ช่วงตรงกับ `calculateRisk` ทุกจุด (ถ้าสองฝั่งหลุดจากกัน Dashboard จะโกหกแบบเงียบๆ) |
| 2026-08-03 | **[Phase 4]** "สถานะงาน" ของ certificate หมายถึงอะไร | สถานะของ **task ล่าสุด** (`DISTINCT ON (certificateId) ORDER BY createdAt DESC`) — ไม่ใช้ `renewalTasks: { some: { status } }` ของ Prisma | cert หนึ่งใบมี task หลายใบตามรอบการต่ออายุ (รอบก่อน COMPLETED รอบใหม่ NEW) · `some` แปลว่า "เคยมี task สถานะนี้" ซึ่งจะทำให้ cert ที่กำลังต่ออายุรอบใหม่ยังถูกนับเป็น "เรียบร้อยแล้ว" · Prisma query builder ทำ correlated subquery แบบนี้ไม่ได้ จึงใช้ `$queryRaw` เฉพาะจุดนี้ (`common/current-task.ts`) |
| 2026-08-03 | **[Phase 4]** วิธีนับตัวเลข Dashboard | query เดียวดึง (cert ในขอบเขต + สถานะ task ล่าสุด) แล้ว**นับใน JS** ด้วย `calculateRisk()` — ไม่เขียนเกณฑ์ 30/60/90 เป็น `CASE WHEN` ใน SQL | เกณฑ์ความเสี่ยงต้องมีที่มาที่เดียว ถ้ามีสำเนาใน SQL วันหนึ่งตัวเลขบน Dashboard จะไม่ตรงกับ badge ในตารางแล้วไม่มีใครรู้ว่าฝั่งไหนถูก · ข้อมูลระดับบริษัทมีหลักร้อย–พันแถว การนับใน JS จึงไม่ใช่คอขวด |
| 2026-08-03 | **[Phase 4]** เส้นทาง transition ที่เพิ่มจากสเปก | เพิ่ม (ก) `In Progress`/`Waiting *` → `Testing` โดยตรง (ข) `Testing` → `In Progress` · `Completed`/`Cancelled` เป็นปลายทาง เปลี่ยนต่อไม่ได้ | (ก) cert ที่ออกเอง (self-signed ซึ่งมีจริงในไฟล์ลูกค้า) ไม่มีขั้นติดต่อ vendor/รอ CA ถ้าบังคับให้ผ่านจะได้ประวัติที่โกหก (ข) ทดสอบไม่ผ่านต้องกลับไปแก้ ไม่ใช่ยกเลิกงาน · ส่วนการ "เปิดงานที่ปิดแล้วใหม่" ให้เปิด task ใบใหม่แทน เพื่อให้แต่ละรอบการต่ออายุมีประวัติของตัวเอง |
| 2026-08-03 | **[Phase 4]** มอบหมายงานที่ยังเป็น `New` | เดินสถานะเป็น `Assigned` อัตโนมัติ และเขียน HistoryLog **แยกอีกบรรทัด** (`ASSIGN` + `STATUS_CHANGE` พร้อม `reason: 'assign'`) | `Assigned` แปลว่า "มีผู้รับผิดชอบแล้ว" ถ้าไม่เดินสถานะให้ board จะมีงานที่มีคนรับแต่ยังขึ้นคอลัมน์ "รายการใหม่" · แยกบรรทัดในประวัติเพราะการเปลี่ยนสถานะที่ระบบทำเองต้องตรวจย้อนได้ ไม่ซ่อนอยู่ในรายการมอบหมาย |
| 2026-08-03 | **[Phase 4]** ใครรับงานได้ | เฉพาะ `ADMIN` / `OPERATOR` ที่ยัง `isActive` — มอบหมายให้ `VIEWER` หรือบัญชีที่ปิดแล้ว → 400 | viewer มีสิทธิ์อ่านเท่านั้น ถ้ามอบหมายได้ก็จะมีงานที่เจ้าของงานเปลี่ยนสถานะเองไม่ได้ |
| 2026-08-03 | **[Phase 4]** action ที่ลงประวัติเวลาเปลี่ยนสถานะ | ใช้ action ตามความหมายจริง: `WAITING_VENDOR`→`CONTACT_VENDOR`, `WAITING_CA`→`CSR_GENERATED`, `TESTING`→`VERIFY`, `COMPLETED`→`COMPLETE`, `CANCELLED`→`CANCEL` ที่เหลือ `STATUS_CHANGE` | ค่า enum เหล่านี้ถูกวางไว้ตั้งแต่ Phase 0 ตามกฎเหล็กข้อ 6 (install/verify/complete) — ถ้าลง `STATUS_CHANGE` ทุกบรรทัด timeline จะอ่านไม่ได้ว่าเกิดอะไรขึ้น · `INSTALL`/`CERTIFICATE_ISSUED` เก็บไว้ให้บันทึกเหตุการณ์แบบระบุเองในเฟสถัดไป |
| 2026-08-03 | **[Phase 4]** ที่เก็บไฟล์แนบ | เก็บไฟล์บนดิสก์ที่ `UPLOAD_DIR` (ไม่ลง DB) · ชื่อไฟล์บนดิสก์เป็น uuid, path ใน DB เป็น **relative** ต่อ `UPLOAD_DIR` · ชื่อเดิมของผู้ใช้เก็บในคอลัมน์ `filename` และคืนตอนดาวน์โหลดผ่าน `filename*=UTF-8''` | ชื่อไฟล์จาก client ไม่ควรมีผลต่อ path ที่เขียนจริง (path traversal) · เก็บ path แบบ relative ทำให้ย้ายที่เก็บ/mount ใหม่ได้โดยไม่ต้องแก้ทุกแถว · มี `resolveStoredPath()` ยืนยันว่า path ยังอยู่ใต้ `UPLOAD_DIR` ทุกครั้งที่อ่านไฟล์ |
| 2026-08-03 | **[Phase 4]** ชนิดไฟล์แนบที่รับ | allowlist ตามนามสกุล (`.pem/.crt/.cer/.der/.csr/.p7b/.p7c` + เอกสาร) และ **ปฏิเสธ `.key/.pfx/.p12/.jks/.pk8`** พร้อมข้อความอธิบาย | ระบบนี้ติดตามวงจรชีวิต certificate ไม่ใช่ที่เก็บ private key · ถ้ารับไฟล์กลุ่มหลังจะกลายเป็นที่รวมกุญแจของลูกค้าทุกรายในเซิร์ฟเวอร์เดียว (การตรวจเชิงลึกอื่นๆ อยู่ใน Phase 8) |
| 2026-08-03 | **[Phase 4]** Certificate เป็น read-only | ไม่มี `POST/PATCH/DELETE /certificates` ในเฟสนี้ — ข้อมูล cert เข้าระบบผ่าน Import เท่านั้น | ตรงกับกฎเหล็กข้อ 1 และการ upsert ของ Phase 3 ที่ไม่ล้างค่าที่ไฟล์ไม่มี · ถ้าเปิดให้แก้มือตอนนี้จะเกิดคำถามว่า import รอบถัดไปควรทับค่าที่คนแก้หรือไม่ ซึ่งควรตัดสินพร้อมหน้าจอจริงใน Phase 7 |
| 2026-08-03 | **[Phase 4]** `$queryRaw` กับชนิดของคอลัมน์ id | เทียบ id เป็น **text** ตรงๆ ห้าม cast `::uuid` ใน raw SQL | Prisma สร้างคอลัมน์ `String @id @default(uuid())` เป็น `text` ไม่ใช่ `uuid` — การ cast พารามิเตอร์เป็น uuid ทำให้ Postgres ตอบ `operator does not exist: text = uuid` (เจอจริงตอนรัน e2e ครั้งแรก unit test ที่ mock prisma จับไม่ได้) |
| 2026-08-03 | **[Phase 4]** ชื่อไฟล์ภาษาไทยจาก multipart | เพิ่ม `decodeUploadFilename()` แปลงชื่อที่ busboy ถอดเป็น latin1 กลับเป็น UTF-8 ที่ขอบทางเข้า (คงค่าเดิมถ้าไม่เข้าเงื่อนไข เช่นชื่อ latin1 จริงอย่าง `café.pem`) | busboy (ที่ multer ใช้) ถอดส่วนหัว `filename` เป็น latin1 ทำให้ `ใบรับรอง.pem` ถูกบันทึกเป็นตัวขยะและผู้ใช้ดาวน์โหลดกลับมาอ่านชื่อไม่ได้ · แก้ที่จุดเดียวตอนรับไฟล์ ไม่ให้ข้อมูลเสียเข้า DB (ถ้าเก็บเสียไปแล้วต้องไปตามแก้ทีหลัง) |
| 2026-08-03 | **[Phase 4]** นับ done/pending บน Dashboard | `done` = task ล่าสุดเป็น `COMPLETED` · `pending` = งานที่ยังไม่ปิด **รวม cert ที่ยังไม่มีงานเลย** · `CANCELLED` ไม่นับทั้งสองฝั่งแต่รายงานแยก | Grouped Bar ในดีไซน์เดิมมี 2 แท่ง (Done/Pending) · cert ที่ยังไม่มีใครเปิดงานคือ "งานค้าง" ในความหมายของผู้ใช้ ส่วนงานที่ยกเลิกไม่ใช่ทั้งเสร็จและค้าง ถ้าเหมารวมเป็น pending ตัวเลขจะดูแย่กว่าความจริง · มีเทสต์คุมว่า `completed + pending + cancelled = total` เสมอ |

## หัวข้อที่ยังไม่ตัดสินใจ (ต้องเคลียร์ในเฟสถัดไป)

| หัวข้อ | เฟสที่ต้องตัดสิน | หมายเหตุ |
|---|---|---|
| ยืนยันวิธี hash รหัสผ่าน (scrypt ตามที่ทำไว้ หรือเปลี่ยนเป็น bcrypt/argon2) | ยังค้าง — ใช้ scrypt ไปแล้วใน Phase 2 | ถ้าเปลี่ยน แก้ที่ `apps/api/src/common/password.ts` ที่เดียว — user ที่มีอยู่ต้อง reset รหัสหรือเขียนโค้ดรองรับ 2 scheme |
| Refresh token / logout (ตอนนี้มีแค่ access token อายุตาม `JWT_EXPIRES_IN`) | Phase 7 หรือ 8 | ถ้าต้องการ logout จริง (เพิกถอน token ก่อนหมดอายุ) ต้องมี token blacklist หรือ refresh token rotation — ปัจจุบันการปิดบัญชี (`isActive=false`) ตัดการใช้งานได้ทันทีอยู่แล้ว |
| Rate limit ของ `POST /auth/login` | Phase 8 | อยู่ใน checklist Phase 8 แล้ว (`ตรวจความปลอดภัยพื้นฐาน: rate limit login`) |
| แก้ข้อมูล certificate ด้วยมือได้หรือไม่ (ถ้าได้ import รอบถัดไปควรทับหรือไม่) | Phase 7 | Phase 4 ทำเป็น read-only ไว้ก่อน — ควรตัดสินพร้อมหน้า Certificate Detail จริง |
| ลบไฟล์แนบ | Phase 7 หรือ 8 | Phase 4 มีแค่ upload/download/list ตาม checklist · ถ้าเพิ่มการลบ ต้องตัดสินว่าลบไฟล์บนดิสก์จริงหรือเก็บไว้เพื่อการตรวจสอบ |
| อัปเป็น Prisma 7 + ย้าย config ไป `prisma.config.ts` | หลัง Phase 8 | ไม่กระทบการทำงานตอนนี้ เป็นแค่ deprecation warning |
